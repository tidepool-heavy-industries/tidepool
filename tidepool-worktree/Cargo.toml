[package]
name = "tidepool-worktree"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Managed git worktrees, durable registry, and typed repository events (PRD 19)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# A normal dependency, not a dev one: `testing` is a pub module so integration
# tests (separate crates) can use the scripted writer without a feature dance.
tempfile.workspace = true
