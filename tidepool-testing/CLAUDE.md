# tidepool-testing — prepared runtime test support

This crate owns shared Haskell test setup, constructor tables, bridge round trips, and the prepared execution corpus runner. Production handlers and wire-record definitions belong to their owning crates.
