# tidepool-testing — prepared runtime test support

This crate owns shared Haskell compile and prepared-runtime test setup. Small
representation fixtures live in `tidepool-test-data`; production handlers and
wire-record definitions belong to their owning crates.
