//! `RecipeCheck` suspension request type — GENERATED by tidepool-protocol, DO NOT EDIT. Regenerate: `cargo run -p tidepool-protocol --bin tidepool-protocol-gen` (see tidepool-protocol/src/gen/mod.rs).

use tidepool_bridge_derive::FromCore;

/// One variant per `RecipeCheck` GADT constructor, named EXACTLY as in Haskell.
/// Decode-only: this effect suspends to the consuming crate's own
/// orchestration rather than an `EffectHandler`, so there is no dispatch
/// glue here — see this module's crate-level generator doc. A field's
/// only job is making the `FromCore` name+arity match correct; the
/// consumer's own roster composition decides which fields (if any)
/// it goes on to read, so an all-recognition, no-field-read effect is
/// expected here, not a bug.
#[derive(FromCore)]
#[allow(dead_code, clippy::enum_variant_names)]
pub enum RecipeCheckReq {
    RecipeRoot,
    RecipeTurn((String, i64, i64), String),
    RecipeActivation,
    RecipeGit((String, i64, i64), Vec<String>),
    RecipeWrite((String, i64, i64), String, String),
    RecipeRead((String, i64, i64), String),
    RecipePresent,
    RecipeNotPresented(String),
    RecipeUnconfirmed(String),
    RecipeAssert(String, bool),
    RecipeRestart,
}
