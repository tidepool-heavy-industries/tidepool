# Contributor workflow

For this crate's ownership boundaries and invariants, see [CLAUDE.md](CLAUDE.md).

Use the focused handler test for the changed effect and compile affected
consumers. Add or update effect operations at their single-source schema and
definition before implementing the generated request here.
