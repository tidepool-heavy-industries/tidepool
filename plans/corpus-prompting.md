# Corpus, recursive delegation, and Git checkpoints

This pass builds on notebook cells at `57def9e0a`. It prepares the next run;
it does not launch one. The implementation owner integrates Git/publication,
executable workspace examples, and the assembled model-facing instructions.

## Contract

- Live-source forks checkpoint eligible Git changes on the source's current
  branch before capture, under existing writer/publication admission. Red code
  is valid. No hooks, signing, checks, private checkpoint refs, or generated
  trailers. Git failure stops that fork; clean sources reuse HEAD.
- Repository-local `/.shoal/` exclusion protects runtime state. Automatic
  staging shares source-import exclusions; nested shipped workspace sources
  remain ordinary tracked code. Explicit refs and committed fallbacks remain
  committed-source operations.
- Shared instructions teach substantial cells, agent-work commits, inherited
  lane selectors, recursive replanning and bounded fresh consultations.
  Roles carry differences. Workspace core replaces the fallback base; both
  append the common API guide.
- Reassess after the third repair at one boundary or eight model rounds with
  no fork or candidate checkpoint. Revise the lanes, ask for authority, or
  justify bounded local completion. No scheduler/counter mechanism is added.
- Existing selective collectors and necessary Silent assignments remain.
  NeedEvidence ends a consultation. Respond-last is teaching, not a syntax ban.
  Lookup searches constrained signatures and reports current-row availability;
  unresolved type parameters remain Unknown. Runtime grants remain separate.

## Implementation and evidence

- [x] Git checkpoint/exclude owner and real-repository tests (4 passed).
- [x] Host publication wiring and mounted root/descendant/sibling/failure checks.
- [x] Representative workspace recipes and corpus migration.
- [x] Deterministic cancellation-at-cell-item-boundary regression.
- [x] Fresh assembled-context review and focused integration checks.
- [x] Attempt full workspace recipes and diagnose the late assertion failure.
- [x] Commit and push this integration pass.

Run focused checks during work and one full recipe suite at integration.
Preserve the overlay 40-generation oracle. Rerun only affected failures after
repairs. Record exact executed checks and unresolved limits below before landing.

### Integration evidence

The integration boundary exercised real-repository Git checkpoints (4 tests),
mounted fork admission including a busy Git source, GHC lookup fidelity (61
tests), hosted current-row lookup availability, notebook cancellation before and
after a committed cell prefix, the original long-sleep sibling test, and the
40-generation overlay oracle. `just fixtures-check` passed all 217 semantic
tests with a refreshed source fingerprint and unchanged CBOR corpus. The full
workspace recipe run reached a late `Project.RoutingChecks.routing` assertion
after its earlier cases passed. A focused rerun showed the actual result was
`ReplyOpen`, preceded by an import binding notice; the assertion now checks
the result suffix. The patched recipe compiled in a second focused rerun, but
that process's exit result was lost when its terminal session was interrupted.
No complete post-fix recipe pass is claimed.

## Next-run briefing

The next dogfood is one human-monitored iteration, with an implementation model
reading the run and applying judgment. No evaluation pipeline is required.
Watch specifically for the previous failure: a lead loses all live children
while continuing local repair rounds. Inspect whether it re-decomposes or asks
for help, whether inherited assignments stay short, and whether consultations
return decisions or precise missing evidence. Do not treat a small task finishing
locally as failure. No new run is authorized by this implementation pass.

Deferred: model-written commit metadata, second-backend swarm actors,
immediate-parent effort inheritance,
and notebook Stage 2.
