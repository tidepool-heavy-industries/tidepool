# Corpus, recursive delegation, and Git checkpoints

This pass builds on notebook cells at `57def9e0a`. It prepares the next run;
it does not launch one. The implementation owner integrates Git/publication,
executable workspace examples, and the assembled model-facing instructions.

## Contract

- Live-source forks checkpoint eligible Git changes on the source's current
  branch before capture, under existing writer/publication admission. Red code
  is valid. No hooks, signing, checks, private checkpoint refs, or generated
  trailers. Git failure stops that fork; clean sources reuse HEAD.
- Repository-local `/.shoal/` exclusion protects runtime state. Automatic
  staging shares source-import exclusions; nested shipped workspace sources
  remain ordinary tracked code. Explicit refs and committed fallbacks remain
  committed-source operations.
- Shared instructions teach substantial cells, agent-work commits, inherited
  lane selectors, recursive replanning and bounded fresh consultations.
  Roles carry differences. Workspace core replaces the fallback base; both
  append the common API guide.
- Reassess after the third repair at one boundary or eight model rounds with
  no fork or candidate checkpoint. Revise the lanes, ask for authority, or
  justify bounded local completion. No scheduler/counter mechanism is added.
- Existing selective collectors and necessary Silent assignments remain.
  NeedEvidence ends a consultation. Respond-last is teaching, not a syntax ban.
  Lookup searches constrained signatures and reports current-row availability;
  unresolved type parameters remain Unknown. Runtime grants remain separate.

## Implementation and evidence

- [x] Git checkpoint/exclude owner and real-repository tests (4 passed).
- [ ] Host publication wiring, mounted root/descendant/sibling/failure checks.
- [ ] Representative workspace recipes and full corpus migration.
- [ ] Deterministic cancellation-at-cell-item-boundary regression.
- [ ] Fresh assembled-context review and final integration checks.
- [ ] Complete workspace recipes, commit and push.

Run focused checks during work and one full recipe suite at integration.
Preserve the overlay 40-generation oracle. Rerun only affected failures after
repairs. Record exact executed checks and unresolved limits below before landing.

### Implementation handoff (2026-09-12)

All edits are currently uncommitted on main at `57def9e0a`; preserve root
`.shoal/` state. No live run is being launched. Remaining work is integration,
test repairs, formatting, final review, commit and push (`--no-verify` authorized).

Completed evidence:

- Git checkpoint real-repository tests: 4/4 passed, including exclusion before
  blob creation and preservation of pre-staged excluded files. A temporary index
  isolates staging; the commit advances the ordinary source branch.
- Mounted root/descendant/sibling/fallback publication test and 40-generation
  overlay oracle both passed together. Re-run the mounted test once after the
  final eligible-path enumeration repair (its Git tests have already passed).
- GHC fidelity checks: 59/59 passed, including constrained lookup and actual
  row Available/Unavailable/Unknown. Rust CBOR decode/lookup sorting tests pass;
  REPL with standalone row absent compiles.
- Fresh Sol corpus review repaired stale tree docs, Low effort example, and
  remaining wide teaching records. Shared source-baseline provenance remains
  independent of a child's actual execution head.

Active delegated work:

- `git_checkpoints` (Sol) owns Haskell lookup and the real hosted lookup test
  `actor_host::lookup_availability_tests::hosted_lookup_uses_actual_actor_row_for_constraint_availability`.
  It is running that test and fixing the old `IntrospectionSearchTest.hs` caller
  of `searchTypeMatches`; compile/run `introspection-search-test` as well.
- `audit_corpus_shape` (Terra) owns cancellation regression, now correctly in
  `tidepool-actor/src/hosted_lifecycle_tests.rs` using a test-only Sleep row.
  The discarded root test failed because production ActorEffects excludes Sleep.
  Run `resident_sleep_waits_fifteen_minutes_without_blocking_a_sibling` after the
  hosted lookup Cargo job finishes. This now includes the two cell-boundary cases.
- `review_corpus` (Sol) is reviewing GHC availability independently; consume its
  findings before landing. Corpus and Rust transport agent work is complete.

Toolchain: reuse `CARGO_TARGET_DIR=/home/inanna/dev/tidepool-overlay-efficiency/target`.
Rust frontend is that target's `debug/tidepool-extract`. The new TPINSP004 worker
is the **main checkout** `haskell/dist-newstyle/.../tidepool-extract-bin` (resolve
with `cabal list-bin tidepool-extract-bin` inside the repository Nix setup).
Do not use the old notebook-takeover worker: it emits TPINSP003. The pinned
frontend may need `TIDEPOOL_ALLOW_STALE_EXTRACT=1` for timestamp checks; build
the final frontend normally before final validation. Unset an inherited daemon
socket when using repository wrappers. Avoid overlapping Cargo builds/runs.

Still required: final real hosted lookup and cancellation passes, final fixture
boundary check with matched worker (private inspection wire is now V4), complete
`shoal check --workspace examples/shoal-workspace --recipes`, prompt/catalog and
new recipe coverage, final formatting/diff review, commit and push. No test failure
may be reported as passing. No new evaluation infrastructure or dogfood launch.

## Next-run briefing

The next dogfood is one human-monitored iteration, with an implementation model
reading the run and applying judgment. No evaluation pipeline is required.
Watch specifically for the previous failure: a lead loses all live children
while continuing local repair rounds. Inspect whether it re-decomposes or asks
for help, whether inherited assignments stay short, and whether consultations
return decisions or precise missing evidence. Do not treat a small task finishing
locally as failure. No new run is authorized by this implementation pass.

Deferred: model-written commit metadata, second-backend swarm actors,
immediate-parent effort inheritance,
and notebook Stage 2.
