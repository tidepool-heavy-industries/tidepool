# Foreground launch execution contract

## Product and exact inputs

Deliver [resident sleep](../../../next/resident-sleep.md) and the existing
[applications wrapup](../applications.md). Engine and shared-server work are
paused. Preserve ordinary full TUIs and every product guarantee.

Selection: `/home/inanna/dev/tidepool/target/command-foreground-runner-20260911/selection.json`.
Main: `f0c079fabd09c35ac7cbb50a3eba74d1a8ea4943`; selected native runtime/tooling
baseline: `80e36633f515b03e11189e8516be21065e73335e`.
Runtime source `a3321960e474b3e0fa8ed00eb1633025d7f7e7b3` identifies the fixed
runner, not a substitute product base. No prepared continuations exist.
R7 applications `8eda2640b5047786f5dcf2af8b7eae9760e5e767`, combined preservation
`0c1fb83f2285775cb16b212ce2e152bbe9a07374`, external Codex
`d84cda697a8dac2842bec09dbd7562a3fab4c926` remain immutable original inputs.
Never merge the combined checkpoint wholesale. Source reconciliation selects
useful behavior, not every historical patch.

## Finished walkthrough

In an actual Codex TUI, the model submits one Haskell sleep of fifteen minutes,
then an observable suffix. No intermediate inference/tool returns; one suffix
and final result. Another actor progresses meanwhile. In the awkward case the
operator interrupts normally: control returns promptly, cancelled suffix never
runs, and a subsequent interaction succeeds. A handler uses the same effect
without concurrent mutation of its own actor. No durability across host death.

Applications: bind the intended native session, submit immutable input once,
observe correlated presentation/completion, fork with completed context, and
retire with accounted custody. Lost acknowledgement, stale generation, compaction
or native/host loss never licenses duplicate execution. Recovery reports lost
live state and only replays safe source. These are retained claims needing
resulting-source proof, not new milestone implementation.

## Executable ownership graph

Initial Astra -> Sol coordinator -> [sleep lead](sleep.md), [applications lead](applications.md).
All execution Sol Medium. Coordinator first warms one useful main owning build
with explicit memory, then admits the two fresh selected-context leads from its
main-descended checkout. Leads investigate only their assigned mechanisms and
publish one committed proposal each before broad implementation. Reconciliation,
target enumeration and warm checks are permitted during this checkpoint.

Coordinator retains substantive combined integration: preserve main's runtime/
resource owners, settle cross-lane contracts, own shared build/pin/fixture changes,
integrate the checked pair with sleep, run invalidated integration seams, and ship
through the existing release workflow without hot-changing this swarm.

Before parallel edits coordinator records exact file/hunk owners:

| Surface | Executing owner / early delivery |
|---|---|
| Haskell sleep effect/schema/generated bridge, actor timer/lifetime | Sleep lead; typed minimum seam before sleep children |
| Native host tools/input control/completion | Applications native integrator; combined Jobs + binding routes before native children |
| Tidepool host custody/recovery/backend transport | Applications lead; current runtime boundary after reconciliation |
| Sleep outer tool-wait interaction with backend/host | Sleep lead specifies required wait/cancel contract; applications lead owns edits in shared backend/host files until explicitly transferred |
| Shared Cargo/flake pins, canonical fixture baseline, combined host wiring | Coordinator incorporates narrow deltas promptly, not after all consumers finish |
| Product sleep guidance | Sleep lead in shipped source; canonical running `.shoal` remains frozen |

Paths above are ownership-map starting points, not verified exact timer
implementation locations. Leads must resolve actual callers and conflicting
hunks in their proposal. A shared-file reservation without an executing owner
and early consumer delivery is not acceptable.

## Single planning checkpoint

Coordinator consolidates both proposals in `proposal.md` here. Each names current
owners, concrete APIs/files, useful second-level branches, shared source/reasoning
at fork, engineering retained by parent, exact checks and challenged assumptions.
For applications include reconciled pair or precise remaining reconciliation
work; implementation forks wait for that pair. For sleep include the complete
outer tool-wait path, not just a Rust timer.

Publish one actionable planning question with the artifact/commit to the initial
planner through the retained review collector. Planner reviews coupled boundaries
once via `updateRequest` on coordinator's pending Delivery. Coordinator commits
incorporation and forwards release to the existing pending leads; record
presentation and resulting-source incorporation, not another request.
After that checkpoint planner drains its collector and idles. Coordinator owns
ongoing reporting and final integration; do not route routine gates back to Astra.

Related children inherit completed shared reasoning and live `boundHead`; taskSource
names the actual committed baseline. Leads/reviewers may use selected contexts.
After integrating children, update source and decision deltas before the next
local frontier. No mandatory worker-stage tree. Small leaves finish directly.

Fresh Astra slots belong to the requesting Sol: sleep continuation/cancellation
or outer transport semantics; applications exact identity/custody or demonstrated
engine coupling. Send compact source + evidence + alternatives and return directly
to that owner. Substantial engine compatibility design or dropping a guarantee
requires a human decision; no such change is preauthorized.

## Acceptance and completion

Follow [verification policy](../verification-prompt.md) and contributor recipes.
Use `Cmd.start/run` with explicit `withMemory` for builds/tests, retained jobs
on overrun, completed warm snapshots before implementation forks. No workspace
batteries, cache clearing, new target directories, or running-harness changes.

Each lane retains exact source, executable selection, commands, selected/executed
counts, observed success/failure and compile-only gaps. Coordinator checks final
sleep + applications tool waiting, steering, recovery, completion and cleanup on
the matched candidate; reuse unchanged exact-source evidence. Run fixtures-check
if translation/serialization changes require it. Actual mock-provider TUI checks
and one real fifteen-minute wait are required; controlled time alone is insufficient.
Only x86_64 execution is available. External RSI owns harness/cache/usage/custody
monitoring; product owners do not reconstruct old live actors.

Completion: checked integrated Tidepool head + external Codex head/pin, accepted
behavior/evidence, explicit remaining limits, release disposition, and short
session/completion/environment ownership handoff for later shared-server work.
Retire incorporated local collectors/workers; preserve pending obligations.
