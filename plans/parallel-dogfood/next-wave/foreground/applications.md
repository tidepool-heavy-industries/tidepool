# Applications owner

Read the current-state inventory and 06-integration checklist. R7 A0–A7 and matched
A8 are retained, not assignments to rebuild. Lead owns Tidepool reconciliation,
host recovery/custody integration and exact paired release.

Prepare a new continuation descended from launch main, preserving original refs.
Inspect applications net changes and any combined-source corrections (registry
test was incorporated in R7 coordinator). Record exact original head/common
ancestor in squash provenance if consolidating. Keep current runtime/resource/
orchestration owners; omit obsolete scaffolds with reasons and retained guarantees.

During reconciliation a useful bounded native Sol integrator handles external
Codex against selected `80e36633f515b03e11189e8516be21065e73335e`. Check ancestry;
do not rewrite redundantly. Prior inventory's `7259e937` conflict list is a hint,
not the launch baseline. Integrate Jobs and durable binding/control in
`codex-rs/tui/src/host_dynamic_tools{.rs,/input_control.rs}` as actual source
requires. Preserve original worktree/ref; work on a new branch. This is source
reconciliation, not permission for pre-baseline implementation forks.

A bounded acceptance inventory can identify exact retained commands/fixtures
without edits while lead reconciles host consumers. Publish paired usable source,
ownership, material omissions/adaptations and first completed owning warm check
before implementation descendants.

Next useful frontier is driven by concrete defects:

* Native integrator retains dispatch/completion/custody joins; from combined
  tool/control shared source may fork focused input-route and command-job
  failure/cleanup work independently.
* Lead retains host recovery wiring; a recovery owner repairs/checks registry
  race and source-only replay against current runtime, while a matched-consumer
  owner adapts existing full-TUI scripted-provider fixtures to the selected pair.
  Do not duplicate checks whose exact-source evidence remains valid.

Fresh independent Sol review examines actual conflict resolutions, admission/
completion ordering and Jobs cleanup on exact candidate. Reuse reviewer for repairs.
Lead runs four actual recovery cases (registry, source-only,
`abnormal_root_reuses_only_a_queue_ready_conversation`,
`forest_operator_survives_model_root_recovery`) plus required input/completion/
retirement failures and matched full-TUI checks. Compile affected runtime/actor/
Tidepool and native test targets, with exact test counts.

Return a reviewed pair with preserved steering, immutable input, presentation/
compaction fences, completion, recovery and cleanup guarantees. Applications can
ship independently of engine; document any real narrow dependency instead of
importing unfinished engine code. Shared-server handoff is documentation only.
