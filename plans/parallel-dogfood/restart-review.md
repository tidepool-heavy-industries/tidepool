# Restart execution release

Reviewed coordinator consolidation `474ef34d5e9fe873a1a4ecb11b6edbfa2e701a63`,
applications readback `935fcef8f72c497e9dba34edc5abf2af20eada55`, and engine
readback `7e3969c4e61cc3fe82ad11f5851f21cabddf4405` at their exact commits.
This is design release, not implementation acceptance.

## Recipient and continuation

The retained Sol coordinator receives this review through its pending Delivery
response. Incorporate this artifact, correct the affected readbacks, and steer
both owned pending lead responses. Verify presentation and incorporation before
dependent forks. Publish one compact incorporation/first-frontier record through
cumulative Attention, then own routine local execution; no second planning round
is required. Preserve the remaining questions, not just the latest event.

## Release and corrections

Release selective A0 adaptation/isolated native fixture port, adapted M0 and
independent real M1/M2 work, followed by the documented recursive local waves
when their shared prerequisites hold. Accept the coordinator's mechanism-level
reservations and both first-child inherited/boundHead/Sol Medium choices. Keep
all A0–A8/M0–M7 obligations, matched-package checks and frozen live tools intact.

1. **Applications scaffold size:** do not implement A1–A3 serially under the
   name of an A0 scaffold. Before the first two A0 siblings, establish the
   minimum existing fixture/API contract, pinned native source and file ownership.
   The *joined* A0 boundary must then provide the narrow real binding/dispatcher
   seam that native admission and host delivery consume. Input lifecycle policy
   and full inbox/deployment transitions belong to their ready production owners.
   Do not let a mock-only contract or a large lead-only foundation defer forks.
2. **Source and behavior:** the primitive recipe is accepted. Select the authored
   worker behavior too: `withInstructions (projectPrompt "task")` for candidate
   implementation, or the appropriate existing component behavior for a Delivery
   owner. Pair the actual reply type with that obligation. Substantial related
   children inherit the current completed reasoning and use coding from boundHead;
   Task.taskSource names the committed scaffold. Never apply withDecision with an
   older decisionSource that rewinds that Task: use the checked incorporation
   source. Keep historical decision origins in evidence, not as the current seed.
3. **Engine local recursion:** M2 owns its substantive root/failure/storage join.
   Establish the exact safepoint-PC rule from owning Cranelift evidence; do not
   paper over missing maps with arbitrary address-range acceptance. Let independent
   storage adaptation/checking proceed once owner types/file boundaries permit,
   rather than waiting for every root repair to finish. The bounded Astra repair
   is authorized if needed, with M2/engine owning incorporation. M1 actual output
   still precedes schema freeze; calling/layout/root agreement precedes M4/M5.
4. **External RSI boundary:** remove application worker assignments for normalized
   provider-request/prefix/cache comparison or build/disk/usage monitoring. External
   RSI owns those observations. Keep product latency/resource settlement tests and
   real fresh-model acceptance with product owners; do not silently drop product
   gates merely because monitoring is external. No infrastructure acceptance tree.
5. **Runtime handoff:** accepted as proposed. Engine must exercise reusable
   language error, unavailable integrity failure and cancelled suspended work at
   the persistent-session consumer before claiming M2 safe; applications consumes
   that checked disposition at A7. Source-only recovery never recreates live values
   or authority. Reserve additional shared edits with coordinator before overlap.

## Remaining questions and later gates

The root has requested human direction on external TPLR compatibility needs and
native aarch64 runner availability. No reply has yet established either. Continue
independent implementation; do not close final migration policy or native ABI
acceptance from assumptions or cross-compilation. Complete Codex-suite permission
is requested at its integration point, not from every worker. Consequential new
architecture or an unsupported harness continuation returns to this root; ordinary
engineering and agreed later forks do not.

## Evidence scope

Read the exact committed proposals. Spot-checked current production owners:
source-only session/recovery.rs, strict TPLR header rejection, exact stack-map
lookup/frame walking, native --destination-local --after-call rendering and
pane-based retirement. These corroborate ownership/risk, not new behavior.
Inspected frozen Project.Work to verify helper context/source behavior and
withDecision semantics; no shared module was changed. Only plan diff checks ran
for this review; implementers own product evidence at their resulting revisions.
