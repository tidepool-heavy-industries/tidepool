# Applications lane: full native TUIs with exact ownership

Own A0–A8 from [the application index](../interactive-applications/README.md) and
[integration checklist](../interactive-applications/06-integration.md). Start with
the readback checkpoint in [the wave allocation](README.md); no implementation
fanout before planner release. The native steering repair is already the baseline.

The useful unit of work is a functioning consumer/failure boundary. Preserve the
full Codex TUI and its embedded native execution owner. Never substitute a daemon
or observer architecture. Conversation identity, current live owner, admission,
presentation, incorporation, process completion and hosted-work settlement are
different facts. Read the detailed mechanism plan when working that boundary.

## Planned recursive frontiers

1. **A0 shared baseline and executable fixture.** Lead establishes the native
   checkout owner, matched source, revisions and real isolated PTY/provider fixture.
   Scaffold the handshake/operation contract against its consumers. Agree runtime
   cleanup/error seams with the engine lead through the coordinator.
2. **Binding and process foundations.** Once the fixture and interfaces are
   usable, fork meaningful Sol obligations for A1 native/live binding and A4's
   independent exact process/terminal mechanism. A1 itself may divide native and
   Tidepool consumers after committing a shared wire contract. Integrate/check
   each frontier; process groundwork is not yet complete launch custody.
3. **Durable input and hosted completion.** After accepted A1, build A2 native
   admission with A3 host consumers following its concrete committed contract.
   A6 native completion ownership can advance on its independent A1 prerequisite.
   Separate native file owners before overlapping native obligations. Integrate
   native and host source at actual consumer boundaries, not speculative stubs.
4. **Custody and full lifetime.** Integrate A4 with launch into A5 and connect A6
   hosted-work retirement. Reconcile concurrent work through the sole host row,
   exact process capability and existing request owners. The lead owns this
   substantive join rather than outsourcing its architectural understanding.
5. **Recovery and matched acceptance.** A7 consumes the checked lifetime and
   delivery mechanisms. Fork genuinely independent recovery/acceptance cases when
   fixtures are shared. A8 incorporates the reviewed native pin through the
   coordinator, removes obsolete paths, and verifies matched packaged behavior.

These are dependency frontiers, not five mandatory worker roles. The Sol readback
should improve the allocation based on actual files and seams. Each subtree can
repeat scaffold/fork/integrate; independent review operates on concrete candidates
and consequential failure paths. Retain reviewers for repairs without queue cycles.

## Declared Astra consultations

- **Admission uncertainty:** at A1/A2 contract formation, a bounded expert can
  review durable identity, restart quarantine and lost-reply reconciliation across
  native and host owners. Deliver a concrete supported decision/contract amendment.
- **Completion/custody join:** before integrating A5/A6/A7, a bounded expert can
  inspect exact completed-call forks, cancellation and retained resource authority.
  Give it real code/checkpoints and conflicting claims, not both entire transcripts.

Use `DesignSlot` / `consultDesign` with `gpt-6-astra`, ordinarily High for these
consequential questions. Sol owns incorporation and implementation. Do not launch
both experts at startup or use an expert merely to summarize a plan.

## Acceptance and reporting

The original A0–A8 checklist remains complete acceptance. Preserve the distinction
between real isolated-PTY/local-provider checks and fresh live-model acceptance.
No candidate deployment into this running swarm. Report exact Tidepool/Codex
commits, actual binaries, migrated records, checks executed versus only compiled,
deleted obsolete paths and remaining product gates. Partial preparation is useful
when explicitly scoped and reviewed; keep the encompassing obligation pending.
