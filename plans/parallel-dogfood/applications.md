# Applications lane

Own [A0–A8](../interactive-applications/06-integration.md). Start with the
[shared decisions](../interactive-applications/README.md), then read mechanism
files 01–05 as their frontier approaches. First deliver the
[plan in your own words](coordination.md#check-the-plans-before-implementation) before implementation.

The existing steering repair is the baseline. Preserve full native Codex TUIs
and their embedded execution owners. The detailed plan owns semantics and tests.

Before planning new implementation, inspect your lane in [saved work](saved-work.md).
Resume its actual unfinished frontier; the table below describes dependencies,
not an instruction to redo completed scaffolds.

## Initial decomposition to refine

| Frontier | Scaffold → useful forks → integration |
|---|---|
| A0 | Matched baseline, revision reservations and real isolated PTY/provider fixture; agree native/host contracts |
| A1 + independent A4 foundation | Fork live-binding consumers and exact process/terminal work after their shared fixtures exist |
| A2/A3 + A6 native work | Durable admission then host consumers; native completion may advance after A1. Assign nonoverlapping native owners |
| A5 + completed A6 | Integrate launch custody, hosted completion and retirement through the existing owners |
| A7 → A8 | Recovery consumes the checked lifetime; fork independent acceptance cases, then matched release/deletion |

Each subtree may repeat scaffold/fork/integrate. These are dependencies, not
mandatory stage workers. Partial process groundwork does not complete custody.
The lead owns the substantive joins and exact native/Tidepool handoffs.

Applications owns an isolated checkout of `/home/inanna/dev/codex`, subject to its
AGENTS.md and actual process authority. Preserve the original checkout. Coordinate
runtime/session seams and shared release inputs with the coordinator.

## Declared Astra slots

- At A1/A2: durable identity, restart quarantine and lost-reply reconciliation.
- At A5/A6/A7: completed-call forks, cancellation and resource custody.

Use `DesignSlot` / `consultDesign`, Astra High, when concrete uncertainty needs it;
supply evidence and a bounded question. Sol incorporates the decision.

Acceptance is the full A0–A8 checklist at matched revisions. Distinguish real PTY
checks, compilation and fresh-model acceptance; retain remaining gates in partial
slices. Candidate tools must not replace this swarm's running baseline.
