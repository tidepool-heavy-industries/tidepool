# Pending planner review and continuation

This is evidence retained in the Tidepool authoring repository. Application paths
below are relative to `/home/inanna/dev/shoal-repl-live-20260907`; hashes identify
source in that repository. Its installed `.shoal` remains unchanged by this RSI
pass. These notes do not become active worker instructions until the next
authorized run preparation incorporates the agreed corrections.

The swarm remains on operator hold. These are prepared review findings, not
implementation release and not evidence that workers incorporated them. After
explicit restart/release authorization, the original planner and Sol owner resolve
the remaining human choice, deliver the scoped corrections, and verify updated
plans/task decisions against actual source before dependent implementation.

## Exact readbacks

| Lane | Committed readback | Document |
|---|---|---|
| RecipeLibrary | `8c75e0d3b57b40e2e87f83ac9c0433cc2035acf6` | `docs/live-workbench/plans/recipe-library.md` |
| RecipeInteraction | `4ef32256dac349f1a1c4c93af1d1e78313415931` | `docs/live-workbench/plans/recipe-interaction.md` |
| RelationExplorer | `b08667e5ddb2bcd476b37d6df2fa981465dd8c69` | `docs/live-workbench/plans/relation-explorer.md` |
| LivingPresentation | `b5b7087870647c03446d417a3db3a63a5605b80d` | `docs/live-workbench/plans/living-presentation.md` |

These commits remain in their branches. Read exact committed content with Git;
being listed here does not claim incorporation into the application HEAD.
The root readback is `docs/LIVE-WORKBENCH-READBACK.md`; `027b243` records initial
planner steering. Product preparation is integrated through `f6d90983`.

## Product choice still owned by the human

The desired first finished messaging flow remains to be clarified: select an
already-running worker, select a model and create a worker, or support both through
the palette. No answer to that question has been recorded. Preserve the original
ambition of useful reusable Haskell helpers; do not silently substitute an unverified
binding form for a finished selected-recipient experience.

## Prepared corrections for the release batch

- **Library:** separate defining a reusable helper from invoking it. Represent
  required capabilities/bindings honestly. Verify a supplied AgentRef's identity
  with the supported public accessor where appropriate; this is not a resolver
  from arbitrary graph keys. Validate actual generated types and execution in the
  authorized isolated context. Reject helper/recipient name capture, validate
  escaping and preserve inference. A mock or golden source string is insufficient.
- **Interaction:** F6 opens the proposed schema-driven palette/form. Insertion
  belongs at the cursor/selection and preserves surrounding draft; retain revision
  race refusal and cancellation. Do not adopt whole-draft replacement as the target
  merely because the scaffold helper implements it. Preview/navigation make no POST.
- **Relations:** preserve the host's actual optional creator field. Reject duplicate
  exact actor keys at the protocol boundary and preserve the last good snapshot.
  Canonical ordering and cycle breaking must be deterministic; retain raw facts
  for inspection, default to supervision, and provide creator/context views.
- **Presentation:** use immutable submitted source, including a narrow read-only
  pending-source accessor where needed, rather than reading the changing editor
  draft. Distinguish local UnknownExecution from provider observation staleness.
  Bound rendering work without silently truncating user history.
- **Shared integration:** root supplies shared UI wiring and reduced-motion controls
  early enough for each lane's acceptance. Define exact APIs and recipients before
  dependent forks. The wiring owner owns timely delivery, not just an edit lock.

These corrections describe the reviewed target, not current implementation.
Resolve objections with evidence before release. Carry the agreed decisions into
current Tasks; `Project.Live` constructors provide the base assignment and do not
by themselves contain every later planner correction. Use exact resulting source
with `withDecision`, whose decisionSource replaces taskSource.

## Continuation across local waves and a new swarm

Each substantial lead explains its next ready frontier and the later integrated
behavior it unlocks. Children can recursively own further local integration loops.
Track remaining feature acceptance across partial commits and context turnover.
Do not repeat delivered contract work merely because the execution context is new.

A new swarm needs current valid handles; old request/actor handles are not portable
proof of response ownership. Carry the obligation and source/decision evidence
through the handoff, then establish actual continuation ownership in the new run.
Keep any useful retained context only through supported operations. A source fix
or candidate definition check is not permission to restart or resume workers.
