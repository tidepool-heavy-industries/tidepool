# Service convergence checkpoint

Accepted production/test source:2db8f2c607fd70ffd5acf5a188cae7c84f1ccb25.
Native-delivery decision97c6722a incorporated as9bfe7acf. This is a root-integrable
preparation checkpoint, NOT the completed actor-owned native service.

## What works in source

Pre-bootstrap exact custody; deterministic sibling/recursive boundHead bootstrap;
opt-in namespace process owner; strict existing-inbox receipt storage; explicit
notification effect/interpreter and receipt observation; socket/build retain-first
ownership; addressable host process-result recovery; two-stage HTTP admission/drain
and exact resident seal consumption; actor-owned admission seal and retained typed
actor/machine cleanup. These have independent reviews and focused integrated checks.

Production still uses legacy tmux/embedded native launch and conservatively retains
custody/resources after submission. Notification send explicitly returns Unavailable.
There is NO production persistent RemoteAppServerClient/controller or native hosted-
call forwarding implementation in this checkpoint. No live host was replaced.

## Exact decisive service checks (not a single full suite)

Logs are in the service worktree target/service-retirement-evidence/.
- e74b3a31: lifecycle-unit-integrated.log,4 passed96 excluded; authored policy
  lifecycle-authored-integrated.log,2 passed0 excluded; bootstrap integration
  lifecycle-bootstrap-integrated.log,1 passed144 excluded (two exact sibling
  SessionReady events plus nested boundHead/reply/watch/retirement).
- 2db8f2c6: http-actual-owner-integrated.log,1 passed145 excluded. Genuine authored
  endpoint/private seal, foreign identity and late dispatch checks; callback with
  empty ready groups is not native durable-prefix proof.
- 42457814: addressable-integrated.log,15 passed130 excluded. Real blocked bwrap,
  production resource-bearing error handoff and receiver loss before pin; socket,
  launch-error aggregation and legacy custody seams. Not full CLI/native inference.
- 17d37211: strict-inbox-integrated.log,20 hit-checked syscall fault subprocesses
  within1 passing parent test; strict-host-integrated.log,2 host regressions passed.
  This is syscall failure propagation/reopen evidence, not physical power loss.
- fe41095c: build-retention-integrated.log,4 passed119 excluded, including retained
  leaf reacquisition denial. Later actor/HTTP changes do not alter that owner.

Full changed test targets compiled through Nix/extractor setup; private daemons
were observed torn down. Retained WaveCheck values preserve exact commands,
revisions, attribution and evidence paths. Earlier custody build evidence includes
an unlaunched Shoal executable; no executable is claimed built at latest source.

## Unaccepted host candidate and bounded repair

b51745d8/6e8167f5 remain OUTSIDE accepted service history. Independent negative
regression paired a stopped real actor with another live actor's opaque service:
terminal-path cleanup could drain the mismatched service. This is reachable fixture
evidence, not proof current normal producer mismatches. Lost-shutdown-waiter
ownership was independently tested and is not the remaining blocker.

Narrow repair in flight: actor owner exposes existing canonical actor-policy
construction; host then accepts actor/config/listener and constructs canonical
endpoint/service internally. Same retained host implementer/reviewer will verify
that invariant and negative regression. No bool/label proof or new dispatcher.
Native/external cleanup and final binding settlement remain separately gated.

## Remaining convergence obligations

Native600be9df is now DELIVERED, not waiting on human work. Root owns matching
flake/Cargo dependency preparation. Retained designer is verifying exact client API
against immutable delivered source. Remaining work: canonical host repair; persistent
native client/controller and completion bridge; registration/readiness and exact
assignment/notification routing; mounted no-TUI/observer/closed-prefix/retirement
canary. Freeze unrelated cleanup expansion and escalate genuinely new architecture.

Most service effort so far went to cleanup/custody prerequisites and repair loops,
not controller implementation. No measured per-phase timing or ETA is claimed.
Retained leads/experts and pending typed watches remain live across this checkpoint.

## Canonical host repair integrated

The pairing blocker above is superseded by reviewed candidate02a2fe316b166460c5e485c4c2cccfd01723664a,
merged as da78e0be0e318811cc543d3f1e3070487cf0cc57. Production constructs the
canonical endpoint from the exact retained actor inside the hosting entry point;
independently supplied endpoints exist only as an untrusted negative-test seam.
Stored seal/shutdown/HTTP operations survive waiter loss. Confirmed exact resident
cleanup permits HTTP drain, not native/external cleanup or final custody release.

Direct integrated check at da78e0be:
`NEXTEST_TEST_THREADS=1 just test-lib tidepool 'test(actor_host::hosted_retirement::tests::)'`
ran9 tests:9 passed,146 skipped,62.345s; run62af53ce-4de7-4e44-945d-66b46be4e83e.
This includes the final repaired foreign initially-terminal endpoint fixture,
live/trusted-terminal paths, lost seal/HTTP/shutdown waiters, unsupported/foreign
seals and failed-child uncertainty. Library test target compiled; repository
Nix/extractor wrapper observed private daemon940778 torn down. Evidence:
service worktree target/service-retirement-evidence/canonical-host-integrated.log.
`cargo fmt --all -- --check` and `git diff --check` passed at integration.
No Shoal executable at this integrated revision was launched.

Native client source handoff is now retained in native-client-handoff.md at2dc3daf1.
Persistent controller/bridge implementation, root matching pin/build, mounted
acceptance and final native/custody settlement remain open. This is still a
preparation delivery, not completion of the service product.
