# STG cutover: accept the review-fix batch, define the next wave

Branch `engine/stg-production-cutover`, HEAD `aca28b7bb`, nothing pushed.
Governing plan: `plans/stg-completion.md` (steps 2–5 remain). Contracts, all
"amended, for review before implementation": `plans/handoff/designs/
{resume-contract-v2,lifetime-contract-v2,actor-exit-contract}.md`. Starting
file map for the production turn: `plans/handoff/README.md:166-190`.

## Context

The 2026-09-15 codex wave (12 commits) was reviewed (`review-2026-09-15.md`)
and its ten blocking items were fixed in five commits (`6567074d9`,
`04e59d567`, `0870da4ff`, `e50831879`, `79e426ce9`). The full prepared corpus
passes for the first time (812 rows; execution 702/0 failed; comparison 234/0).
This plan (1) records the Fable review of that fix batch, (2) closes the last
open gate item (the three `just verify` timeouts), and (3) defines the wave
that finishes step 2 of the cutover — one real prepared notebook turn through
the production owners with typed resume — plus what step 3 (lifetime) needs
to start alongside it. Opus implements; Fable is re-engaged only at the
review points marked **↑Fable**.

## Part 1 — Review of the fix batch (Fable, read at `aca28b7bb`)

Verdict: **accept all five**. Each landed with a test proven to fail with the
fix withheld; no floor or fixture was weakened.

| Commit | Judgment call reviewed | Verdict |
|---|---|---|
| `e50831879` classifier | Dropped the separate `UnclassifiedOccurrence` reason; a bare verb `Var` (eta-reduced alias, higher-rank argument, cast/tick head) classifies with zero args → `OpenSiteType` → deferred rejection "result type is unresolved". | Correct. GHC eta-reduces `runLLMTurn @a` to a bare `Var`, so a distinct "used as a value" reason would misfire on legitimate open-type aliases. The invariant is stated at the enforcement site (`PreparedSites.hs:66-75`). `openEta = runLLMTurn @Bool` (type args present, zero value args) still rewrites to a PAP of the sited sibling — the closable first-class case the peer required. |
| `6567074d9` ROOT + reservation | Item 6 (descriptor interning) closed with **no code change**: every program has ≥1 top slot, `install_staged` checks `TopSlotBaseMismatch` before `absorb`, so the slot base *is* the compile reservation; a second outstanding compile is refused with no side effect. | Correct today, and pinned by `an_outstanding_compile_is_refused_after_another_consumes_its_reservation`. Note: lifetime-contract decision 5 (per-program root blocks) *removes* `TopSlotBase`, and with it this reservation. The interner then needs its own transactional story — carried into Wave B below as an explicit item, not rediscovered. |
| `04e59d567` daemon | Lock released at `retire()` (after unlinking own inode) so a replacement binds while the old daemon drains. Settlement kept indeterminate on EOF/failed write; `explicit_rejection` preferred over a failed write. | Sound. Order unlink→release-lock means a replacement holding the lock never sees the retiring inode. `remove_dead_endpoint` unlinks only a socket inode observed refusing, re-stat'ed unchanged, under the lock. Unix-socket backlog-full blocks rather than refuses, so a live-but-busy daemon is never mistaken for dead. `retired: Cell<bool>` makes the double `retire()` on the stamp-change path idempotent. Two accepted costs: a read-only socket directory now fails start (lock file creation), and `<socket>.lock` is never removed (documented; unlinking a lock file is the classic two-holder bug). |
| `0870da4ff` oracle | Sealed payload digest; `no_oracle` capped at the measured 464. | Correct; the cap is a ceiling, and `validate_oracle_domain` already refuses value oracles on compiler-introduced keys, so both directions are closed. |
| `79e426ce9` resolver | `MachineState.prepared_byte_pools` as the single `Addr#` authority; O(programs × log literals) per call. | Correct and the right owner (`vmctx` and `ObservationHeap::external_owner` both reach it). `resolve_literal_bytes` holds a `RefCell` borrow across the closure — safe today because closures only call `PinnedBytes` methods; a comment should say so (folded into Wave B item 3, which touches this field anyway: pools must carry a `ProgramId` owner so retirement can drop them — lifetime decision 1/7 step 8). |

Follow-ups deferred from the review (Part 2B of the previous plan) are
re-homed below under "Small follow-ups"; none blocks the wave.

## Part 2 — Close the gate item: the three `just verify` timeouts

Solo reruns (`just test-lib tidepool 'test(=<name>)'`, one at a time):

| Test | Solo result | Classification |
|---|---|---|
| `research_policy_tests::preview_and_explicit_research_budget_match_without_spawning_during_preview` | PASS, 64.6 s (nextest "slow") | Load starvation under the four-slot `ghc-heavy` group. |
| `tests::lifecycle_sources_follow_replacement_and_capture_retained_exit` | PASS, 58.7 s | Load starvation. |
| `tests::active_update_keeps_original_request_and_fences_terminal_delivery` | **FAIL**, 49.7 s, deterministic, `actor_host.rs:5478` | **Not a timeout.** The cell `pollRequestUpdate clarification` renders as `Right (UpdateQueued)`; the assertion expects `Right UpdateQueued`. Same drift as the two `Right result` → `Right (result)` fixture edits in `56f030122` (`command_jobs_tests.rs:651,993`), which the codex wave accepted as "current contracts". |

Root cause (confirmed from source): the cell `output` is rendered by
`render_cell_observation` (`tidepool-actor/src/resident_workbench.rs:2779-2809`)
→ `TidepoolInspection.displayPageWithout … (obs ())` → the overlappable
`Display a => PageDisplay` instance (`haskell/lib/Tidepool/Inspection.hs:248`)
→ **`displayTree`**. `instance Display (Either a b)` (`Inspection.hs:108-114`)
hardcodes `treeParts "Right (" ")" [displayTree value]` — no precedence
argument, no atomicity check — while the same instance's `displayWith`/
`displayWithout` emit `"Right "` with no parens. Introduced by `57def9e0a`
(notebook cells + retained display pages) when the cell path moved from
`displayWith` to `displayTree`; unchanged since. Same shape in `Maybe`
(`:102`, so `Just 5` → `Just (5)`), `ResponseState` (`:281-283`),
`WatchState` (`:287-288`), `Settlement` (`:291-292`), `ProgressState`
(`:296,298`). `56f030122` accepted the drift on two fixtures rather than
fixing it. Not an STG defect; does not gate the wave.

**Fix (Opus, one commit, product-side Haskell):** the test contract is
GHC-derived `Show` layout, so implement it rather than a heuristic: add
`displayTreePrec :: Int -> a -> DisplayTree` to `class Display`
(`Inspection.hs:64-79`) with default `displayTreePrec _ = displayTree`;
container instances (`Either`, `Maybe`, `ResponseState`, `WatchState`,
`Settlement`, `ProgressState`) render children at precedence 11 and wrap in
parens only when `p > 10` *and* the child is a constructor application (the
`Show a => Display a` fallback uses `showsPrec p`, so nullary constructors and
atoms stay bare). Then revert the two `56f030122` needles
(`command_jobs_tests.rs:651,993`) to `Right result`; `actor_host.rs:5479`
passes unchanged. Rebuild the deployed stdlib (`haskell/CLAUDE.md`), then
`just fixtures-check`. Focused runs: the three affected tests solo, plus
`Tidepool.Inspection` unit coverage (`Right UpdateQueued`, `Right (Just 5)`,
`Just (-1)` per GHC, `Left "text"`).

Record both tables in `implementation-2026-09-15.md` (replacing "The three
timeouts are unclassified until each reruns alone") and in
`stg-completion.md` step-1 evidence. Task #13 (a quiet full `just verify`) is
**dropped**: lint, fixtures, the full corpus and every touched crate's focused
suite have passed; the gate's remaining value was these three tests, now
classified. Re-run the broad gate once at the Wave A exit, not before.

## Part 3 — The next wave

### Where step 2 stands (from source, `aca28b7bb`)

Production-wired: compile/install inside codegen (`machine.rs:357`
`compile_for_install`, `:399` `install` transaction), `PreparedRuntime`
install+bind+lease API (`session/prepared.rs:485,583,597`), value handles and
realm close (`machine.rs:790,835`), observation to `tidepool_bridge::Value`
(`observe.rs:443-576`), typed-site classification on the projection side.
Everything that makes it a *notebook* is test-only or missing:

- **No engine route.** `workbench.rs` has no engine selector (its `Prepared`
  at `:262` is an effect-receipt disposition, unrelated). The production
  registry is `ActorMachineRegistry<H,O> = SessionRegistry<ResidentSession<H,O>,
  String>` (`resident_workbench.rs:194`, constructed at
  `resident_actor.rs:6439`) — hardwired to Core. `impl ActorRunTarget for
  PreparedRuntime` (`mount.rs:283`) has **no production caller**; its
  `Error = Infallible` (`:279`) because nothing is installed. `PreparedRuntime`
  reaches only `SingleSlot` in tests (`placement_retirement.rs:309`,
  `prepared_resident_composite.rs:389`) — review item 4 of `stg-completion.md`.
- **No extractor turn mode.** `request.rs` has `Target` and
  `RetainedGeneration` (tag 38, `MAGIC = TPREQ007`) but no `PreparedTurn`;
  `CompiledTurn` (`turn.rs:592`) has no `prepared` field. The test driver
  `SessionTurns`/`TurnForm` (`prepared_turn.rs:41,78`; doc `:20-25`: "no GHC
  turn classification and no IO/bind-effect turn semantics") drives
  `--target`. `turn::prepared_turn_module` (`turn.rs:380`) emits `module X
  where` + imports + body — no pragmas, preamble, effect stack or
  `ActorCompileView` imports (the dialect gap at `stg-completion.md:329-335`).
- **No suspension, let alone resume.** The prepared machine has zero freer
  awareness (no `freer_names`/`Union`/`E`/`Val` in `prepared_program/`) and
  asserts it never parks (`machine.rs:842` `debug_assert!(closed.frames.
  is_empty(), "PreparedMachine never parks a continuation…")`; `prepared.rs:178`
  `frames` always 0). `set_actor_execution` (`prepared.rs:841-851`) stores
  `EffectRunPolicy`/`LivePayloadPolicy` and never acts on them; `prepared.rs:271`
  "no effect handlers of its own yet". Site evidence (`pmYieldSites`,
  `PreparedStg.hs:113`) is computed and dropped; `SCHEMA_VERSION = 9` has no
  site table → the resume contract lands as **schema 10**. The only resume is
  the test fixture's `resumeInt` (`FreerResume.hs:43-45`) driven by
  `prepared_execution.rs:597` with hardcoded `DataConId`s.
- **No render.** `render.rs`/`inspection.rs`/`view.rs` never see a
  `PreparedResult`. And the cell render itself is a *compiled display turn*
  (`render_cell_observation`, `resident_workbench.rs:2779`, compiles
  `displayPageWithout … (obs ())`), so on the prepared route that turn must
  project too — `Tidepool.Inspection` under prepared STG is unproven (pure
  Prelude/Effects/Lens templates are proven, `implementation-2026-09-15.md:105`).
- **Lifetime** still the bump-claim model (`claimed_slots`, `TopSlotBase`,
  `SESSION_TOP_SLOTS = 4096`, `ProgramId` = vector index `machine.rs:740`, no
  uninstall, old space "compacted only on an explicit major pass this machine
  never runs" `machine.rs:1063`). `MonotonicIdIssuer` already issues
  `binding_ids` (`prepared.rs:260`) — reuse it for `ProgramId`.
- **Actor exit**: the `actor shutdown failed` rewrite is still at
  `local_actor.rs:1439`.
- **Test tier**: `.config/nextest.toml:16` excludes all `tidepool-runtime`
  integration binaries from the default tier; `prepared_turn.rs` is
  additionally `#[ignore]` (needs `$TIDEPOOL_EXTRACT`). Any new end-to-end test
  runs under `--ignore-default-filter` in a `ghc-heavy` slot.

### Wave A — one complete prepared notebook turn (step 2, first slice)

Goal (step-2 exit, narrowed to the first slice): a real notebook session on
an explicit migration route declares a function, applies it on a later turn,
performs `runLLMTurn @Bool` through the production request path, parks, is
resumed by the session resume API with JSON `true`, and completes — with
receipts and the committed prefix preserved. Core is never a fallback after a
turn starts.

Ordered items; each is one commit with its failing test first.

**A1. Extractor prepared turn mode** (file map `README.md:166-190`).
Nullary `PreparedTurn` request field, tag 39, `MAGIC` bump (`TPREQ007`);
Haskell `ExtractRequest.hs` fold/encoder/decoder; `app/Main.hs`
`runTurnMode`/`compileVariants` compiles with `PreparedStg` + the request's
retained generations and writes one `__result.prepared.cbor` (tuple for
multi-binder binds). Rust `CompiledTurn.prepared: Option<PreparedProgram>`
read in `read_compiled_turn`; `TurnRequest.{retained, prepared}` through
`run_turn_with_pin`; all current callers pass `prepared: false`. Test:
round-trip beside `retained_generation_round_trips_through_the_typed_protocol`;
a `--turn` request with the field set yields the sidecar.
**A2. One template owner, session-owned install path.** The prepared turn
module is rendered by `resident_workbench_templates` (`workbench.rs:606`) —
real pragmas, preamble, effect stack, `ActorCompileView` imports — with the
prepared variant differing only in its result binding; `turn::
prepared_turn_module` (`turn.rs:380`) is deleted. Fold `SessionTurns`'
retained-set derivation, identity extraction, artifact parse and install into
`session/prepared.rs` behind `PreparedRuntime`; delete `SessionTurns`,
`TurnForm`, `project()`. Rewrite `tests/prepared_turn.rs` on the shared
templates + `eval_import_lines` (three `run_turn`s + a two-name bind; model
`turn_classification_corpus_old_and_new_path_agree`), register it in
`tests/suites/`, toolchain-gated like `prepared_execution.rs:34`. First probe
(before A2's code): project one `displayPageWithout` render module and one
dialect-sensitive expression (defaulting, `OverloadedStrings`) through the
production compile view — the render turn must project or A3 has no output.
**A3. Explicit migration route reaching the production registry.** Make the
registry's machine type a route choice at session construction:
`ActorMachineRegistry` over an engine enum (`ResidentSession` | `PreparedRuntime`,
both already `ActorRunTarget`), chosen once, never switched after the first
turn; `mount.rs:279` `Error = Infallible` becomes the real install error.
`resident_workbench` dispatches cell items to the prepared route through the
same `TurnKind`/ordered-cursor path; prepared results reach
`render_cell_observation` as a compiled display turn on the same engine;
prepared bindings drop the `ScopeId::ROOT` placeholder (`prepared.rs:433`) for
the session realm; `set_actor_execution`'s stored policies are either acted on
or removed (no stored-and-ignored policy). Test: a session on the prepared
route runs A2's three turns through `resident_workbench`, renders `it` in a
cell receipt, and on a failing turn reports `ResidentError::Run` with the
committed prefix intact and **no** Core fallback.
**A4. Schema 10: site table with delivery mode and structural type evidence**
(resume decisions 1–3). `VerbSpec.vsDelivery`; `YieldSite` records
`HostAnswer wire | LiveReentry | ExitCellFill | TerminalCapture`; `types:
[TypeNode]` hash-consed, normalized in `TypePolicy` (synonyms, families,
newtype erasure), `HostUnconstructible` leaves; constructor closure interned
for every reachable `Data` node; `DescriptorInterner.by_host` index refusing
divergent `host_id`s; validation of node/ctor resolution and field reps.
Fixtures regenerate through `just fixtures-update` + the seven prepared
fixtures recipe (`FreerRetention.md`). Tests: codec round-trip; schema 9
rejected; `Either Int Text` ≠ `Either Text Int`; a never-matched `Left`
declared.
**A5. One resume entry, one settlement routine** (decisions 4–5).
`Tidepool.Internal.Resume.resumeLifted = qApp` (NOINLINE, deployed stdlib);
generated `settleEff` per compiled row in `tidepool-mcp` beside the `*Sited`
helpers, forcing request fields except `CoreValue` bindings; the prepared
turn template admits both tops. Rust: the prepared machine gains freer awareness
through the interned `Val`/`E`/`Union` identities (`tidepool_repr::freer_names`,
as `effect_machine.rs:111-210` does for Core — one name table, not a copy);
the `machine.rs:842` never-parks assertion is replaced by real parking. Initial
and resumed completion share one routine — `Val` → completion policy; `E` →
observe the `Union` payload, read the site id, look up the site row, root `q`
+ live payload, park in the machine's `ResourceLedger` with
`FrameEvidence::Prepared { program, site }`. Test: the `FreerResume` fixture
resumed through `resumeLifted` (drops `resumeInt` from the resume path;
fixture keeps it as oracle); `prepared_resident_composite.rs:351` park/resume
now goes through the ledger.
**A6. Host-built answers with rollback** (decisions 6–8).
`prepared_program/answer.rs` on `marshal_descriptor_object`: size → reserve
once (`prepared_gc_trigger`) → bytes via `allocate_external_storage` with a
rollback list → build bottom-up → publish as a realm handle; any failure
resets the bump cursor and releases payloads. Peek → validate → build → take
→ enter in `resume(id, input)`; failures before `take` leave the frame
parked. First slice covers nullary/scalar answers only (`Bool`); `Text`/
`Integer` bytes are slice 2. Tests (acceptance list in the contract):
wrong-family, swapped `Either`, out-of-closure constructor, host answer to
`LiveReentry`, other-realm/`Evaluating` handle, injected allocation failure —
each leaves the frame parked, root/handle counts unchanged, latch clear;
second resume → `UnknownContinuation`.
**A7. End-to-end.** `b <- runLLMTurn @Bool "q"; pure (not b)` on the
prepared route parks, the session resume API supplies `true`, the turn
completes with `false`; `"yes"` and `1` are rejected with the frame still
resumable. Then the step-2 exit scenario minus deferred parts: declare,
retain, apply a PAP on a later turn, park, sibling work, resume, lookup.

**↑Fable review points:** after A4 (schema shape is a one-way door) and after
A6 (the allocation/rollback protocol is engine-invariant work per memory:
Fable drives hard GC work directly — A6 may be implemented by Fable rather
than Opus if the builder touches nursery cursors).

Deferred from Wave A (explicitly, per the contract): `Text`/`Integer`
construction; `Either InvocationExit T` / fanout lists; handle and framed
answers and harness `resume expr` routing; `LiveReentry`/`ExitCellFill`/
`TerminalCapture` wiring; `Map`/`Set`; stale-incarnation admission;
retirement of parked frames.

### Wave B — bounded residency at machine level (step 3, first slice)

Can start alongside A4–A6 (different owners: `prepared_program::machine`,
`plan`, `emit`, `tidepool-heap::gc::raw`). Scope is the contract's first
slice: decisions 4 (monotonic `ProgramId`, programless `PreparedMachine::
empty`), 5 (per-program `RootBlock`, `iconst(block)+load(local*8)` in
`emit.rs`, `TopSlotBase`/`claimed_slots`/`vmctx.prepared_tops` removed), 6
(`quiesce() -> Quiescent<'_>` gate on `call_depth==0`, no rust roots, no
old-space custody, GC state installed, `Reusable`), 3 (owner sets on
`prepared_callables`/`prepared_enters`/`descriptor_registry`/
`DescriptorSpace`/interner rows + live-header census), 2 (major mark over
nursery, old space and statics in `raw.rs` beside `cheney_copy`; old-space
compaction via `stage_compaction`/`commit_compaction` extended to
descriptor arenas), 7 (retirement order), 8 (receipt with pins only).
Add, from the Part 1 review: `prepared_byte_pools` rows carry their
`ProgramId` and are dropped at step 8; and because decision 5 removes the
slot-base reservation, `compile_for_install` must **keep** a typed refusal
of a second outstanding compile (or share canonical descriptors) — the
`an_outstanding_compile_is_refused_…` test is the guard; do not let it be
deleted with `TopSlotBase`.
Test: the 10k-iteration synthetic loop (install k importing k-1, run, retain
a PAP over k-1, release k-1, quiesce+collect, flat counters after warm-up,
apply the surviving PAP at the end). Deferred: edge (c) byte-storage
programs stay pinned and reported; parked sites (d); external sweep from old
space; `PreparedRuntime` lease re-keying and shadowed-binding policy (notebooks
keep no-reclamation).
**This is engine-invariant GC work: Fable implements B directly** (memory:
`fable-drives-hard-gc-work-directly`), with Opus on A in parallel. Wave B is
the reason to upshift again after Wave A's A1–A3 are in.

### Wave C — actor exit contract (approved decision, small, Opus)

`actor-exit-contract.md` recommendations Q1-B and Q2-B: after a drain is
requested a handler failure is terminal (`fail_handler` skips the pause when
`state.drain != Open`); the exit kind is the requested kind and cleanup is a
separate retained fact (delete the rewrite at `local_actor.rs:1436-1442`);
one shutdown deadline computed once in `finish_actor` and threaded to hook
admission, realm/placement checkout (`checkout_wait`) and children
(`deadline - margin`); `publish_exit` as the single publisher with
`ExitAuthority::{Own, SupervisorForced}` and the all-children-exited
`debug_assert`. Seven `start_paused` unit tests as listed in the memo.
Independent of A/B; do it whenever a slot is free. Document on `drainActor`.

### Small follow-ups (from the review; none blocking)

- `CallerResult` offer set: record offered instances explicitly, then seed
  from demands (call/`Enter`, PAP completion, joins, imports, retained
  callers, `Returns([LiftedRef])`).
- `emit.rs:305/325`: filter scrutinee-visible joins on the declared contract;
  tests for non-empty alternatives, conditionally-bottoming scrutinee,
  `NoSuccess` join whose body returns.
- Double formatting: pin the 4,888-input oracle as a fixture under
  `fixtures-check`; share `needs_precedence` (`host_fns/primops.rs:748` vs
  `formatting.rs:58`); `num-bigint` → `workspace = true`.
- `render_child_budget(Option<u32>)` helper in `tidepool-actor`.
- `machine.rs:265` rollback keyed on `programs.is_empty()` (moot after Wave B
  decision 4); `insert_idle(Box<M>)`; `hosted_lifecycle_tests.rs:480`
  `[available]` assertion; daemon `count(1)` true minimum size.
- Cell compile-count reduction (`designs/cell-compiles.md`) — product
  performance, not cutover-critical; schedule after step 4.

### Steps 4–5 (unchanged, after A–C)

Parity under the notebook dialect, prepared route as the default for fresh
sessions, then the deletion ledger. Not planned in detail here.

## Verification

- Every item: failing test first (withhold the fix hunk, run the one test,
  restore), then `just test-lib <crate> 'test(<module>)'` for the owning
  crate, `cargo fmt --check`, crate-scoped clippy `-D warnings`.
- Haskell items (A1, A4, A5): `cabal test prepared-stg-pipeline-test`, then
  `just fixtures-check`; A4 regenerates the seven prepared fixtures per
  `FreerRetention.md` and reseals the corpus oracle if inputs change.
- Wave A exit: `tests/prepared_turn.rs` (registered, toolchain-gated) and the
  A7 end-to-end pass; then **one** broad `just verify`, quiet, and its totals
  recorded in `implementation-2026-09-15.md` and `stg-completion.md` step-2
  evidence. Solo-rerun any timeout before classifying it.
- Wave B exit: the 10k loop's counters flat; full `cargo nextest run -p
  tidepool-codegen` (the crate's own charter check); `just fixtures-check`.
- No broad battery during implementation; focused runs only (memory:
  `feedback-focused-test-runs`).

## Operating model

Opus: Part 2 record-keeping, the rendering fix, Wave A1–A5, Wave C, small
follow-ups. Fable: Wave A6 if it touches nursery cursors, all of Wave B, and
the two ↑Fable review points. No Sonnet/Haiku implementer swarms on engine
invariants (memory). CPU-heavy Haskell compiles serialized.
