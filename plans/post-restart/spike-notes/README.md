# realm-spike working notes

Findings feeding `plans/post-restart/realm-verdict.md`. One file per
investigation lane; each empirical claim carries its receipt (the named run and
the counts it produced).

- `realm-checklist.md` — realm-ownership checklist scored item by item, plus the
  per-fragment dispatch-metadata question (spike step 3).
- `realm-lifetime.md` — the two suspected real costs: persistent-root retirement
  and compiled-function lifetime.
- `realm-prototype.md` — the falsifier + cycle-scoping results (written on the
  prototype branch).
