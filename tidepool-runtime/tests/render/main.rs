//! Consolidated `render` theme binary: JSON/format rendering regression
//! guards, merged from separate top-level test binaries into one.
mod fmt_nonfinite;
mod fmt_spec_reject;
mod named_field_render;
mod render_large_ints;
mod user_union_normalize;
