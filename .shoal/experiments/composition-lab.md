# Composition lab — 2026-09-18

Live notebook experiments; illustrative observations, not calibrated evaluations.
Bindings remain in the current root, not across restart.

## Semantic Git microscope

`microscopeLog` retains `git log -18 --format=%H%x09%s` command evidence.
Pure Haskell parsed identities and subjects; one J.each packet screened them
against explicit investigation intent. Code opened up to three candidates above
an experimental 0.75 reading threshold using argv, retaining full patches.
`microscopeRun` retains all judgments and fetched evidence or explicit failures.
Two subjects passed: 10094d205 (eligibility), 4eb47e36d (after-turn implementation).

Repeating full patches in three question strings exhausted the prepared runtime
observation budget (100000). Recovery showed no lost bindings. A revised packet
put complete patches once in shared state and keyed six short questions by OID.
`microscopeShared` succeeded:

| Patch | Startup eligibility | Observer cleanup | Live-provider proof |
|---|---:|---:|---:|
| 10094d205 | .88 | .45 | .06 |
| 4eb47e36d | .23 | .90 | .05 |

These are model judgments about historical patches, not current-code verification.
The lesson: share evidence once; put questions, not duplicated source, in batteries.

## Pure gates plus semantic review

Four synthetic reports: bounded focused-test report, compile-to-live overclaim,
explicit review-criteria injection, and a command with no observed exit.
Initial injection wording assigned the plain overclaim .69. Separating questions
about unsupported claims and explicit directions to the reviewer changed the
plain overclaim's redirect score to .13; explicit injection scored .95.
This single comparison does not isolate stochastic variance or establish accuracy.

`EvidenceStage`, `ReviewRoute`, and `routeEvidence` separate supplied verification
state from semantic suspicion. Twelve counterfactual routes were evaluated.
`cannotPromote` was True: no sampled suspicion score promoted NotRun,
CompiledOnly, or FocusedPassed to ReadyForHumanReview. These stages are toy inputs,
not authenticated test receipts or a production release gate.

## Choices carrying effectful continuations

`preparedReads` names three fixed repository sources. J.choice chose among them
for an explicit task, with an unresolved alternative. J.settle J.careful ran only
the winning handler, which executed argv-based rg against that prepared path.
The selector source was selected and read successfully. `chosenRead` retains the
judgment; `chosenReadResult` retains successful output or explicit error.
No shell strings or paths were generated by Jev.

## Promising follow-ups

- Promote the microscope into a reusable module after testing irrelevant intent,
  missing Git output and no-match paths; never discard command failures.
- Compare shared-state and per-row evidence on a small repeated evaluation set.
- Feed typed command outcomes, not model interpretations, into review-state reducers.
- Explore record actors that accumulate judgments without waking System 2.

## Record actor: completed experiment

ReviewLab retained typed evidence plus Jev judgments in private state. Five sends
(four unique report identities and one duplicate) produced four entries. The
handler checked identity before asking Jev. Results (overclaim, redirect):
observed (.25,.04), overclaim (.94,.06), injected (.93,.97), uncertain (.15,.04).
The interrupted tool call had in fact completed: retained bindings confirmed its
actor and snapshot; no effects were replayed. R.finish returned Completed with
all four entries. No agent/model turn was required per report.

Papercut: a helper with a concrete Handler state type failed in generated wrapper
code because Control.Monad.Freer.State.State lacked its qualified import. Splitting
declarations into a small cell and importing that module qualified allowed it.
Luna worker assigned ownership investigation rather than blindly adding imports.

## Bounded Luna nudge experiment in progress

Explicit evidence-reviewer role, child-only hooks, maximum one automatic self-nudge.
Claim token must be consumed before sending; uncertain delivery is not retried.
Only explicit LAB synthetic evidence can trigger. Parent will separately check
hook delivery, judgment, notification, response and cleanup; a synthetic success
is not proof of general usefulness or production loop safety.
