# Declared Astra leaf: faithful relation forests

Placement: `gpt-6-astra`, Medium, fresh selected context through `relationDesign`.
Owner: the Sol contract lead; result: `DesignAnswer`. This is the wave's planned
expert placement, not permission to start an expert tree.

Inspect the current client relation projection and its consumers. Resolve the
small shared types/semantics needed to switch between creation, supervision and
context ancestry. Decide how the view retains every node with deterministic
ordering under reordering, missing parents, self-links, longer cycles and distinct
incarnations. Explain how raw relationship evidence remains accessible when a
display edge must be omitted. The default remains supervision.

Give the Sol owners enough concrete detail to implement fluently: ownership map,
proposed signatures, one awkward example, and decisive checks. Avoid inventing
a general graph framework, runtime ownership inference, new backend endpoints,
or a second actor registry. Existing native graph data already carries optional
creator; use the provided client protocol contract, not harness archaeology.

Return Decision with evidence when the existing plan is adequate. If recording
new shared semantics requires a plan change, commit it and return AmendPlan with
the exact base, candidate, paths and affected obligations. Stay available for
precise followups. Do not kill useful reasoning at an arbitrary token target.
