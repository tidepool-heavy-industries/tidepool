//! `Finalize` effect declaration — GENERATED by tidepool-protocol, DO NOT EDIT. Regenerate: `cargo run -p tidepool-protocol --bin tidepool-protocol-gen` (see tidepool-protocol/src/gen/mod.rs).

/// The `Finalize` effect's static Haskell-side metadata.
#[must_use]
pub fn finalize_decl() -> crate::EffectDecl {
    crate::EffectDecl {
        type_name: "Finalize",
        description: "Terminate the current Agent turn loop and hand a typed value UP to the parent `runLLMTurn` hole, in-heap (no JSON round-trip — the value may be a closure or other non-serializable value). `finalize x` never resumes; the harness driver reads the value directly and resolves the parent hole via `run_child`.",
        prompt_card: Some("`finalize @T value` — commit the typed answer; this ends your whole agent session and delivers `value` to whoever asked (the parent's `fork`/`runLLMTurn` call site), in-heap."),
        constructors: &[
            "FinalizeWith :: Int -> v -> Finalize v a",
        ],
        type_defs: &[],
        extra_imports: &[],
        helpers: &[
            "{-# OPAQUE finalize #-}\nfinalize :: forall v a effs. Member (Finalize v) effs => v -> Eff effs a\nfinalize v = finalizeSited 0 v",
            "{-# OPAQUE finalizeSited #-}\nfinalizeSited :: forall v a effs. Member (Finalize v) effs => Int -> v -> Eff effs a\nfinalizeSited sid v = send (FinalizeWith sid v)",
        ],
        type_params: &[
            "v",
        ],
        default_row_args: &[
            "Void",
        ],
        helpers_row_polymorphic: true,
    }
}
