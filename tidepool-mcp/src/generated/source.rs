//! `Source` effect declaration — GENERATED by tidepool-protocol, DO NOT EDIT. Regenerate: `cargo run -p tidepool-protocol --bin tidepool-protocol-gen` (see tidepool-protocol/src/gen/mod.rs).

/// The `Source` effect's static Haskell-side metadata.
#[must_use]
pub fn source_decl() -> crate::EffectDecl {
    crate::EffectDecl {
        type_name: "Source",
        description: "Reload the workspace's own Haskell source inside this run. Write a module under a configured source root with ordinary file tools, then `reloadSource []` re-reads every configured root, compiles the whole affected module graph, and — only if all of it typechecks — makes that snapshot the revision LATER cells compile against. It is a compilation boundary, not dynamic scope: the cell that asked for the reload was already compiled against the previous revision and keeps it for the rest of its own computation, and a value bound before the reload keeps the code it was built from. `ReloadRejected` is an ordinary result, not a catastrophe — the edited files are still on disk exactly as written, the previously compiled graph is still active, and the receipt names the snapshot that failed and carries its diagnostics. `sourceStatus` answers what is active and what is on disk as data, so a program can decide whether it is running the current code rather than parsing a status page.",
        prompt_card: None,
        constructors: &[
            "SourceReloadWith :: [Text] -> Source (Either SourceError ReloadOutcome)",
            "SourceStatusWith :: Source (Either SourceError SourceStatus)",
        ],
        type_defs: &[
            "data SourceModule = SourceModule { sourceModuleName :: Text, sourceModuleDigest :: Text } deriving (Show, Eq)",
            "data SourceRevision = SourceRevision { revisionIdentity :: Text, revisionGeneration :: Int, revisionModules :: [SourceModule] } deriving (Show, Eq)",
            "data SourceStatus = SourceStatus { statusActive :: SourceRevision, statusDisk :: SourceRevision } deriving (Show, Eq)",
            "data ReloadOutcome = ReloadUnchanged SourceRevision | ReloadPublished SourceRevision SourceRevision [Text] | ReloadRejected SourceRevision SourceRevision Text deriving (Show, Eq)",
            "data SourceError = SourceUnavailable Text | SourceUnreadable Text deriving (Show, Eq)\ninstance ToJSON SourceError where\n  toJSON e = case e of\n    SourceUnavailable detail -> object [\"tag\" .= (\"SourceUnavailable\" :: Text), \"detail\" .= detail]\n    SourceUnreadable detail -> object [\"tag\" .= (\"SourceUnreadable\" :: Text), \"detail\" .= detail]\n",
        ],
        extra_imports: &[],
        helpers: &[
            "-- | Re-read every configured source root, check the affected module graph,\n-- and publish it as one transaction. `reloadSource []` is the whole\n-- workspace; the list names ADDITIONAL modules to pull into the checked\n-- graph when a module you rely on is not reachable from the workspace's\n-- configured module list. Natural spelling: `Right outcome <- reloadSource\n-- []`. The definitions become available to LATER cells — the cell that\n-- called this keeps the environment it was compiled in, so do not expect\n-- to call a newly loaded API from here.\nreloadSource :: forall effs. Member Source effs => [Text] -> Eff effs (Either SourceError ReloadOutcome)\nreloadSource = send . SourceReloadWith",
            "-- | The revision later cells compile against, and the revision the source\n-- roots hold right now. Compare `revisionIdentity`s to tell whether an\n-- edit is waiting, and look a module up in `revisionModules` to compare\n-- one module across the two.\nsourceStatus :: forall effs. Member Source effs => Eff effs (Either SourceError SourceStatus)\nsourceStatus = send SourceStatusWith",
        ],
        type_params: &[],
        default_row_args: &[],
        helpers_row_polymorphic: true,
    }
}
