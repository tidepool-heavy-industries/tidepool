//! `AskUser` effect declaration — GENERATED by tidepool-protocol, DO NOT EDIT. Regenerate: `cargo run -p tidepool-protocol --bin tidepool-protocol-gen` (see tidepool-protocol/src/gen/mod.rs).

/// The `AskUser` effect's static Haskell-side metadata.
#[must_use]
pub fn askuser_decl() -> crate::EffectDecl {
    crate::EffectDecl {
        type_name: "AskUser",
        description: "Present a typed form to a HUMAN OPERATOR and block until they submit. `askUser @T` presents a human form and returns `T`. Define `T` using ordinary records and constructors, derive `Generic` and `FromJSON` for it and any nested custom types, and end fields in `Text`, `Int`, `Double`, or `Bool`. Constructors are choices, record fields are named inputs, and `Maybe a` is optional; a bad submission re-prompts internally, so there is no `Either` to unwrap. The type IS the form — there is no second description of the shape to drift from the answer type, and the submission is ordinary JSON read back by that same generic `FromJSON`:\n  data Env = Development | Staging | Production deriving (Generic, FromJSON)\n  data Deploy = Deploy { service :: Text, env :: Env, replicas :: Int, note :: Maybe Text } deriving (Generic, FromJSON)\n  d <- askUser @Deploy   -- then read fields with record-dot: d.service, d.env\nFor alternatives that exist only as runtime values, `choose :: [(Text, a)] -> M a` and `chooseMany :: [(Text, a)] -> M [a]` take (label, value) pairs. `askUserRaw :: Value -> M Value` is the raw escape hatch these are built on, carrying the form spec as JSON directly. `note :: Text -> M ()` posts markdown-ish text to the operator's feed WITHOUT blocking — use it to explain what you are about to ask and why, before presenting a form.",
        prompt_card: Some("`choose :: [(Text, a)] -> M a` — labeled decision from (label, value) pairs; ALWAYS prefer it for a decision, the label is the only text the operator sees. `chooseMany :: [(Text, a)] -> M [a]` — pick a subset.\n`askUser @T :: M T` — form derived from `T`'s own shape: a record's fields become named inputs, a SUM's constructors become the choices (nullary constructors are direct options; a payload constructor is a selectable branch with its fields), or a primitive (`Text`/`Int`/`Bool`); a bad submission re-prompts internally, no `Either` to unwrap: `d <- askUser @Deploy`, `k <- askUser @NextStep` then `case k of ...` to sequence follow-ups. A type you define for this needs `deriving (Generic, FromJSON)`. Prefer RECORD syntax for a sum's payload constructors — `Other { detail :: Text }` shows the operator a real label, where `Other Text` only shows a generic `Contents` field; a constructor with SEVERAL positional fields is rejected outright (no names to key each input by), so record syntax is required once there is more than one field.\n`note \"...\" :: M ()` — non-blocking narration to the operator's feed; call it BEFORE presenting a form to explain what you're about to ask and why (it never costs a turn)."),
        constructors: &[
            "AskUserWith :: Value -> AskUser Value",
            "NoteWith :: Text -> AskUser ()",
        ],
        type_defs: &[],
        extra_imports: &[
            "import Tidepool.Form",
        ],
        helpers: &[
            "askUserRaw :: forall effs. Member AskUser effs => Value -> Eff effs Value\naskUserRaw spec = send (AskUserWith spec)",
            "noteRaw :: forall effs. Member AskUser effs => Text -> Eff effs ()\nnoteRaw text = send (NoteWith text)",
        ],
        type_params: &[],
        default_row_args: &[],
        helpers_row_polymorphic: true,
    }
}
