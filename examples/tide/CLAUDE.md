# tidepool-tide — the Tide language interpreter demo

**Charter.** Belongs: a second worked example of the JIT workflow — a small
interpreted language (pest-based `ast`/`parser`) with its own hand-rolled
`Console`/`Env`/`Fs`/`Net`/`Repl` effect handlers. Does NOT belong: production
effect handlers (`tidepool-handlers`).
